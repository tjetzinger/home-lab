---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: moltbot-tls
  namespace: apps
  labels:
    app.kubernetes.io/name: moltbot
    app.kubernetes.io/part-of: home-lab
    app.kubernetes.io/managed-by: kubectl
spec:
  secretName: moltbot-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - moltbot.home.jetzinger.com
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: moltbot-ingress
  namespace: apps
  labels:
    app.kubernetes.io/name: moltbot
    app.kubernetes.io/part-of: home-lab
    app.kubernetes.io/managed-by: kubectl
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`moltbot.home.jetzinger.com`)
      services:
        - name: moltbot
          port: 18789
  tls:
    secretName: moltbot-tls
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: moltbot-ingress-redirect
  namespace: apps
  labels:
    app.kubernetes.io/name: moltbot
    app.kubernetes.io/part-of: home-lab
    app.kubernetes.io/managed-by: kubectl
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`moltbot.home.jetzinger.com`)
      services:
        - name: moltbot
          port: 18789
      middlewares:
        - name: https-redirect
          namespace: apps
